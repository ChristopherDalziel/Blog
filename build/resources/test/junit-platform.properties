# Test classes are only invoked once per test in Kotlin, below allows us to change this default behavior and instantiate test classes one time per class
# Inturn giving us the ability to write the beforeAll and afterAll test cases

junit.jupiter.testinstance.lifecycle.default = per_class